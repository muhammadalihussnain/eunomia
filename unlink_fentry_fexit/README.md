step 1
navigate to the directory 




step 2 
run the command "make"


step  3
run the command "sudo ./fentry_fexit"


step 4
open another terminal and create newfile anywhere in that terminal linke "touch newfile.txt"


step5
run another terminal and run "sudo su -" to reach the root user and then run the command "cat /sys/kernel/tracing/trace_pipe"




step 6 
run the command "make clean"